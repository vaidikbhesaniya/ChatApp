import { Metadata } from "next";

const metadata: Metadata = {
  title: "gather.io",
  description:
    "Gather is a chatting space that lets multiple users hold separate conversations in parallel, walking in and out of those conversations just as easily as they would in real life.",
  icons: {
    icon: "/favicon.ico",
  },
  creator: "Priyansh",
  category: "Community",
  keywords: ["Gather", "Chat", "Messaging", "Community"],
};

export default metadata